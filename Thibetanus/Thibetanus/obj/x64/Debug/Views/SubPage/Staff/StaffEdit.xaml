<Page
    x:Class="Thibetanus.Views.SubPage.Staff.StaffEdit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Thibetanus.Views.SubPage.Staff"
    xmlns:vms="using:Thibetanus.ViewModels.SubPage.Staff"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <vms:StaffEditViewModel x:ConnectionId='2' x:Name="viewModel"/>
    </Page.DataContext>
    <Grid>
        <Grid.Resources>
            <Style x:ConnectionId='3' x:Name="listTextBlock" TargetType="TextBlock">
                <Setter  Property="TextBlock.MinWidth" Value="150"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter  Property="TextBox.MinWidth" Value="150"/>
            </Style>
            <Style TargetType="ComboBox">
                <Setter  Property="ComboBox.MinWidth" Value="150"/>
            </Style>
            <Style TargetType="Button">
                <Setter  Property="Button.MinWidth" Value="100"/>
            </Style>
        </Grid.Resources>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <!-- Large window VisualState -->
                <VisualState x:ConnectionId='4' x:Name="LargeWindow">
                    <!-- Large window trigger -->
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource LargeWindowSnapPoint}"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- Adjust the zoom slider to fit small windows-->                      
                        <Setter Target="spinfo.Orientation" Value="Horizontal" />
                        <Setter Target="txtname.Margin" Value="30,0,0,0" />
                    </VisualState.Setters>
                </VisualState>

                <!-- Medium window VisualState -->
                <VisualState x:ConnectionId='5' x:Name="MediumWindow">
                    <!-- Medium window trigger -->
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource MediumWindowSnapPoint}"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- Adjust the zoom slider to fit small windows-->
                        <Setter Target="spinfo.Orientation" Value="Vertical" />
                        <Setter Target="txtname.Margin" Value="0,0,0,0" />
                    </VisualState.Setters>
                </VisualState>

                <!-- Small window VisualState -->
                <VisualState x:ConnectionId='6' x:Name="SmallWindow">
                    <!-- Small window trigger -->
                    <VisualState.StateTriggers >
                        <AdaptiveTrigger MinWindowWidth="{StaticResource MinWindowSnapPoint}"/>
                    </VisualState.StateTriggers>

                    <!-- Small window setters -->
                    <VisualState.Setters>
                        <!-- Adjust the zoom slider to fit small windows-->
                        <Setter Target="spinfo.Orientation" Value="Vertical" />
                        <Setter Target="txtname.Margin" Value="0,0,0,0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <TextBlock x:ConnectionId='7' x:Name="title" Text="员工编辑" TextAlignment="Center" 
                   FontSize="18"  VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,50,0"/>
        <Grid Margin="50,50,50,50">
            <Grid.RowDefinitions>
                <RowDefinition Height ="300"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ListView x:ConnectionId='8' x:Name="infolist" Grid.Row="0" ItemsSource="{Binding StaffInfos}" IsItemClickEnabled="True"  SelectionMode="None" HorizontalContentAlignment="Stretch">
                <ListView.HeaderTemplate>
                    <DataTemplate>
                        <ListViewHeaderItem>
                            <StackPanel Orientation="{Binding ElementName=spinfo,  Path=Orientation}" HorizontalAlignment="Center" VerticalAlignment="Top" >
                                <TextBlock Text="编号"  Style="{StaticResource listTextBlock}"/>
                                <TextBlock Text="人名"  Style="{StaticResource listTextBlock}"/>
                                <TextBlock Text="权限"  Style="{StaticResource listTextBlock}"/>
                                <TextBlock Text="所属店铺"  Style="{StaticResource listTextBlock}"/>
                                <TextBlock Text="技能"  Style="{StaticResource listTextBlock}"/>
                            </StackPanel>                          
                        </ListViewHeaderItem>
                    </DataTemplate>
                </ListView.HeaderTemplate>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <!--<ListViewItem>-->
                            <StackPanel Orientation="{Binding ElementName=spinfo,  Path=Orientation}"  HorizontalAlignment="Center" VerticalAlignment="Top" >
                            <TextBlock Text="{Binding Code}" Style="{StaticResource listTextBlock}"/>
                            <TextBlock Text="{Binding Name}" Style="{StaticResource listTextBlock}"/>
                            <TextBlock Text="{Binding Role}" Style="{StaticResource listTextBlock}"/>
                            <TextBlock Text="{Binding Salon}" Style="{StaticResource listTextBlock}"/>
                                <ListView ItemsSource="{Binding Skills}"  HorizontalContentAlignment="Stretch">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <!--<ListViewItem>-->
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource listTextBlock}"/>
                                            <!--</ListViewItem>-->
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        <!--</ListViewItem>-->
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <StackPanel Grid.Row="1"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,10,0,0">
                <TextBlock  Text="员工"  FontSize="18"  TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <StackPanel x:ConnectionId='9' x:Name="spinfo" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="30,10,30,0">
                    <TextBox Header="编号:" />
                    <TextBox x:ConnectionId='15' x:Name="txtname" Header="人名:" Margin="30,0,0,0"/>
                    <ComboBox Header="权限:"  Margin="{Binding ElementName=txtname,  Path=Margin}" />
                    <ComboBox Header="所属店铺:"  Margin="{Binding ElementName=txtname,  Path=Margin}"  />
                </StackPanel>
                <TextBlock  Text="员工技能" Margin="30,30,0,0" FontSize="18"  TextAlignment="Left" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <StackPanel Orientation="{Binding ElementName=spinfo,  Path=Orientation}"  HorizontalAlignment="Center" VerticalAlignment="Top" Margin="30,10,0,0">
                    <ListView Grid.Column="0" ItemsSource="{Binding Skills}" IsItemClickEnabled="True"  SelectionMode="None" HorizontalContentAlignment="Stretch">
                        <ListView.HeaderTemplate>
                            <DataTemplate>
                                <ListViewHeaderItem>
                                    <StackPanel Orientation="{Binding ElementName=spinfo,  Path=Orientation}"  HorizontalAlignment="Center" VerticalAlignment="Top" >
                                        <TextBlock Text="编号"  Style="{StaticResource listTextBlock}"/>
                                        <TextBlock Text="项目组"  Style="{StaticResource listTextBlock}"/>
                                        <TextBlock Text="名称"  Style="{StaticResource listTextBlock}"/>
                                    </StackPanel>
                                </ListViewHeaderItem>
                            </DataTemplate>
                        </ListView.HeaderTemplate>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <!--<ListViewItem>-->
                                    <StackPanel Orientation="{Binding ElementName=spinfo, Path=Orientation}"  HorizontalAlignment="Center" VerticalAlignment="Top" >
                                    <TextBlock Text="{Binding Code}" Style="{StaticResource listTextBlock}"/>
                                    <TextBlock Text="{Binding Group}" Style="{StaticResource listTextBlock}"/>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource listTextBlock}"/>
                                    </StackPanel>
                                <!--</ListViewItem>-->
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center"  HorizontalAlignment="Center" >
                        <TextBlock Text="》》添加 》》" FontSize="18" />
                        <TextBlock Text="《《移除《《 " FontSize="18"  Margin="0,40,0,0" />
                    </StackPanel>
                    <ListView Grid.Column="2" ItemsSource="{Binding StaffInfos[0].Skills}" IsItemClickEnabled="True"  SelectionMode="None" HorizontalContentAlignment="Stretch">
                        <ListView.HeaderTemplate>
                            <DataTemplate>
                                <ListViewHeaderItem>
                                    <StackPanel Orientation="{Binding ElementName=spinfo,  Path=Orientation}"  HorizontalAlignment="Center" VerticalAlignment="Top" >
                                        <TextBlock Text="编号"  Style="{StaticResource listTextBlock}"/>
                                        <TextBlock Text="项目组"  Style="{StaticResource listTextBlock}"/>
                                        <TextBlock Text="名称"  Style="{StaticResource listTextBlock}"/>
                                    </StackPanel>
                                </ListViewHeaderItem>
                            </DataTemplate>
                        </ListView.HeaderTemplate>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <!--<ListViewItem>-->
                                    <StackPanel Orientation="{Binding ElementName=spinfo,  Path=Orientation}"  HorizontalAlignment="Center" VerticalAlignment="Top" >
                                    <TextBlock Text="{Binding Code}" Style="{StaticResource listTextBlock}"/>
                                    <TextBlock Text="{Binding Group}" Style="{StaticResource listTextBlock}"/>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource listTextBlock}"/>
                                   </StackPanel>
                                <!--</ListViewItem>-->
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
                <Button x:ConnectionId='10' x:Name="update" Content="更  新" HorizontalAlignment="Center"></Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

